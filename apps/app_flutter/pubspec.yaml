name: app_flutter
description: App principal do ElectroBIM
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  canvas_engine:
    path: ../../packages/canvas_engine

  electrical_engine:
    path: ../../packages/electrical_engine

  normative_engine:
    path: ../../packages/normative_engine

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true