name: normative_engine
description: >
  Package Dart puro que encapsula as regras da ABNT NBR 5410:2004
  aplicáveis ao dimensionamento de instalações elétricas de baixa tensão.
  Sem dependências externas — publicável no pub.dev.
version: 1.0.0
repository: https://github.com/electrobim/normative_engine

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies: {}

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
